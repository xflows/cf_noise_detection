from setuptools import setup

setup(
    name='cf_noise',
    version='0.1',
    include_package_data=True,
    license='MIT License',
    description='Package which includes the widgets from noise and perfeval categories, and other related widgets.',
    
    # install_requires=[
    #       'scikit-learn>=0.14.1',
    # ],
    author='darkoa',
    author_email='',
)
